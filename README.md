# To-Do List Application

Современное веб-приложение для управления задачами с регистрацией пользователей, категориями и красивым интерфейсом.

## Описание проекта

Это полнофункциональное приложение для управления задачами (to-do list), разработанное с использованием современных технологий:

- **Frontend**: React.js с Vite
- **Backend**: Laravel (PHP)
- **База данных**: MySQL
- **Стилизация**: Современный CSS с градиентами и анимациями

## Основные возможности

- ✅ Регистрация и авторизация пользователей
- ✅ Создание, редактирование и удаление задач
- ✅ Отметка задач как выполненных
- ✅ Создание и управление категориями задач
- ✅ Фильтрация задач по категориям
- ✅ Приоритеты задач (высокий, средний, низкий)
- ✅ Красивый современный интерфейс с боковой панелью
- ✅ Адаптивный дизайн для мобильных устройств

## Структура проекта

```
homepage_kaspars_uzd/
├── src/
│   ├── pages/           # Страницы приложения
│   │   ├── Login.jsx    # Страница входа
│   │   ├── Register.jsx # Страница регистрации
│   │   └── Dashboard.jsx # Главная страница с задачами
│   ├── components/      # Переиспользуемые компоненты
│   │   ├── Sidebar.jsx      # Боковая панель с категориями
│   │   ├── TaskList.jsx     # Список задач
│   │   ├── TaskForm.jsx     # Форма создания/редактирования задачи
│   │   └── CategoryForm.jsx # Форма создания категории
│   ├── utils/
│   │   └── api.js       # Настройка axios и API методы
│   ├── routes.tsx       # Маршрутизация
│   └── App.jsx          # Главный компонент
├── backend-laravel-homepage/ # Laravel backend
│   ├── app/
│   ├── database/
│   │   └── migrations/
│   └── routes/
│       └── api.php
└── package.json
```

## Установка и запуск

### Требования

- Node.js (v18 или выше)
- PHP 8.1+
- Composer
- MySQL/MariaDB
- XAMPP/Laragon/OpenServer (для локальной разработки)

### Frontend

1. Установите зависимости:
```bash
npm install
```

2. Запустите dev сервер:
```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Backend (Laravel)

1. Перейдите в папку backend:
```bash
cd backend-laravel-homepage
```

2. Установите зависимости:
```bash
composer install
```

3. Скопируйте `.env.example` в `.env`:
```bash
cp .env.example .env
```

4. Настройте базу данных в `.env`:
```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=your_database_name
DB_USERNAME=root
DB_PASSWORD=
```

5. Сгенерируйте ключ приложения:
```bash
php artisan key:generate
```

6. Запустите миграции:
```bash
php artisan migrate
```

7. Запустите сервер:
```bash
php artisan serve
```

API будет доступно по адресу `http://127.0.0.1:8000`

## Настройка базы данных

1. Откройте phpMyAdmin (`http://localhost/phpmyadmin`)
2. Создайте новую базу данных (например, `homepage_kaspars_todo`)
3. Укажите имя базы в `.env` файле Laravel
4. Запустите миграции: `php artisan migrate`

## API Endpoints

### Аутентификация
- `POST /api/register` - Регистрация пользователя
- `POST /api/login` - Вход в систему
- `POST /api/logout` - Выход из системы

### Категории
- `GET /api/categories` - Получить все категории
- `POST /api/categories` - Создать категорию
- `PUT /api/categories/{id}` - Обновить категорию
- `DELETE /api/categories/{id}` - Удалить категорию

### Задачи
- `GET /api/tasks` - Получить все задачи (опционально: `?category_id=1`)
- `GET /api/tasks/{id}` - Получить задачу по ID
- `POST /api/tasks` - Создать задачу
- `PUT /api/tasks/{id}` - Обновить задачу
- `DELETE /api/tasks/{id}` - Удалить задачу
- `PATCH /api/tasks/{id}/toggle` - Переключить статус выполнения

## Особенности дизайна

- 🎨 Современный градиентный дизайн
- 🌈 Цветные категории для лучшей организации
- ✨ Плавные анимации и переходы
- 📱 Адаптивная верстка для всех устройств
- 🎯 Интуитивно понятный интерфейс
- 🔔 Визуальная обратная связь при взаимодействии

## Технологии

- **React 19** - Библиотека для создания UI
- **React Router 7** - Маршрутизация
- **Axios** - HTTP клиент для API запросов
- **Vite** - Сборщик и dev сервер
- **Laravel** - PHP фреймворк для backend
- **MySQL** - Реляционная база данных

## Разработка

Проект находится в активной разработке. Для подключения к реальному API раскомментируйте соответствующие строки в компонентах и убедитесь, что Laravel API настроен правильно.

## Автор

Kaspars Uzd

## Лицензия

MIT
